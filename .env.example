# =============================================================================
# AI Content Director / AI Marketing Platform – Environment Variables
# Copy to .env and fill values. Do NOT commit .env.
# =============================================================================

# -----------------------------------------------------------------------------
# Runtime
# -----------------------------------------------------------------------------
# Tên ứng dụng (logging, docs). Bắt buộc: No. Mặc định: ai-content-director
APP_NAME=

# Bật debug (SQL echo, verbose). Bắt buộc: No. Mặc định: false. Ví dụ: true | false
DEBUG=

# -----------------------------------------------------------------------------
# Database (Postgres)
# -----------------------------------------------------------------------------
# Connection string Postgres. Bắt buộc: Yes (cho API + workers + migrations).
# Lấy: tự tạo DB hoặc dùng docker-compose (postgres:5432).
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Ví dụ: postgresql://postgres:postgres@localhost:5432/ai_ecosystem
DATABASE_URL=

# -----------------------------------------------------------------------------
# Redis & Queue
# -----------------------------------------------------------------------------
# URL Redis (RQ, cache). Bắt buộc: Yes (cho workers + planner/content cache).
# Lấy: docker-compose redis:6379 hoặc Redis cloud.
# Format: redis://HOST:PORT/DB. Ví dụ: redis://localhost:6379/0
REDIS_URL=

# Tên queue RQ. Bắt buộc: No. Mặc định: default. Ví dụ: default
REDIS_QUEUE_NAME=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Level log (DEBUG, INFO, WARNING, ERROR). Bắt buộc: No. Mặc định: INFO
LOG_LEVEL=

# Xuất log dạng JSON (true) hay text. Bắt buộc: No. Mặc định: true
LOG_JSON=

# -----------------------------------------------------------------------------
# AI / LLM (DeepSeek)
# -----------------------------------------------------------------------------
# API key DeepSeek cho content/planner. Bắt buộc: No (không có thì dùng stub).
# Lấy: https://platform.deepseek.com . Format: sk-...
# Ví dụ: sk-...
# DEEPSEEK_API_KEY=

# -----------------------------------------------------------------------------
# Content / Planner cache
# -----------------------------------------------------------------------------
# TTL cache Redis cho content/planner (giây). Bắt buộc: No. Mặc định: 86400
CONTENT_CACHE_TTL_SECONDS=

# =============================================================================
# Meta / Facebook – Graph API (Page publishing, System User)
# =============================================================================
# Lấy từ: https://developers.facebook.com/apps → App Dashboard
# Bắt buộc cho đăng bài Page: META_APP_ID, META_APP_SECRET, FACEBOOK_PAGE_ID, FACEBOOK_PAGE_ACCESS_TOKEN

# App ID của Meta App. Bắt buộc: Yes (khi bật Meta integration)
META_APP_ID=

# App Secret của Meta App. Bắt buộc: Yes. Bảo mật: secret, không commit
META_APP_SECRET=

# Business Manager ID (nếu dùng Business Manager). Bắt buộc: No
META_BUSINESS_ID=

# System User ID (token không hết hạn). Bắt buộc: No (có thể dùng Page token)
META_SYSTEM_USER_ID=

# Facebook Page ID cần đăng bài. Bắt buộc: Yes (khi publish)
FACEBOOK_PAGE_ID=

# Page Access Token (hoặc token từ System User). Bắt buộc: Yes. Bảo mật: secret
FACEBOOK_PAGE_ACCESS_TOKEN=

# Webhook: token để Meta verify endpoint. Bắt buộc: No (chỉ khi bật webhook)
WEBHOOK_VERIFY_TOKEN=

# Webhook: secret để ký/verify payload. Bắt buộc: No
WEBHOOK_SECRET=

# =============================================================================
# Security / future
# =============================================================================
# Key mã hóa (ví dụ payload). Bắt buộc: No. Format: 32-byte hex hoặc base64
ENCRYPTION_SECRET_KEY=

# Key ký JWT. Bắt buộc: No. Bảo mật: secret
JWT_SECRET_KEY=

# =============================================================================
# OpenAI (future-proof)
# =============================================================================
# API key OpenAI. Bắt buộc: No. Lấy: https://platform.openai.com
OPENAI_API_KEY=

# =============================================================================
# Nhóm chưa dùng trong code (để sẵn cho Phase sau)
# =============================================================================

# -----------------------------------------------------------------------------
# Storage / S3 (chưa dùng)
# -----------------------------------------------------------------------------
# S3_BUCKET=
# S3_REGION=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# -----------------------------------------------------------------------------
# Email / SMTP (chưa dùng)
# -----------------------------------------------------------------------------
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=

# -----------------------------------------------------------------------------
# Webhook / Discord (chưa dùng)
# -----------------------------------------------------------------------------
# WEBHOOK_URL=
# DISCORD_WEBHOOK_URL=

# -----------------------------------------------------------------------------
# Security bổ sung (chưa dùng)
# -----------------------------------------------------------------------------
# SECRET_KEY=
# CORS_ORIGINS=

# -----------------------------------------------------------------------------
# Proxy (chưa dùng)
# -----------------------------------------------------------------------------
# HTTP_PROXY=
# HTTPS_PROXY=
# NO_PROXY=
